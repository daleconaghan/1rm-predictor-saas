services:
  - type: web
    name: 1rm-predictor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: 1rm-predictor-db
          property: connectionString

databases:
  - name: 1rm-predictor-db
    databaseName: 1rm_predictor
    user: 1rm_user